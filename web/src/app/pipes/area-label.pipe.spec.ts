import { AreaLabelPipe } from './area-label.pipe';

describe('AreaLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new AreaLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
