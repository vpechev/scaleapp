import { IncorrectAnsweredQuestionColorPipe } from './incorrect-answered-question-color.pipe';

describe('IncorrectAnsweredQuestionColorPipe', () => {
  it('create an instance', () => {
    const pipe = new IncorrectAnsweredQuestionColorPipe();
    expect(pipe).toBeTruthy();
  });
});
