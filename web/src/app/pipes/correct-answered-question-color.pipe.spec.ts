import { CorrectAnsweredQuestionColorPipe } from './correct-answered-question-color.pipe';

describe('CorrectAnsweredQuestionColorPipe', () => {
  it('create an instance', () => {
    const pipe = new CorrectAnsweredQuestionColorPipe();
    expect(pipe).toBeTruthy();
  });
});
