import { ComplexityColorPipePipe } from './complexity-color-pipe.pipe';

describe('ComplexityColorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ComplexityColorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
