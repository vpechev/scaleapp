import { ComplexityColorPipe } from './complexity-color-pipe.pipe';

describe('ComplexityColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ComplexityColorPipe();
    expect(pipe).toBeTruthy();
  });
});
